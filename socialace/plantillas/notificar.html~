{% extends 'base.html'%}
{% block content%}
				<script>
	$(function() {
		$( "#datepicker" ).datepicker();
	});
	</script>


<!-- title -->
<div id="page-title">
	<span class="title">Mis notificaciones</span>
	<span class="subtitle">Estas son las solicutudes de amistad, comentarios de album o de contenido.</span>
</div>
<!-- ENDS title -->

<!-- Posts -->
<div id="posts">

<!-- post -->
<div class="post">
	{% for campo in informacion %}
	<h4>{{ campo.historia.usuario.first_name }}
       		{{ campo.historia.usuario.last_name }}</h4>
	<!-- shadow -->
	<div class="thumb-shadow">
		<!-- ENDS post-thumb -->
		<div class="the-excerpt">
	   		{{ campo.descripcion }}
	   		 <form name="form3"  action="."  method="post" enctype="multipart/form-data">
	      	 {% csrf_token %}
			 <label>
			 	 <input  class="form-control" placeholder="amigos" type="text" value="{{ campo.historia.usuario.username }}"></input>
			 </label>
			 <br>	
   	 		 <label>
           		 <button class="btn btn-primary dropdown-toggle" type="submit" name="aceptar" value="{{ campo.historia.usuario.username }}" id= "{{ campo.historia.usuario.username }}">Aceptar</button> 
     		 </label> 
     		 <label>
           		 <button class="btn btn-primary dropdown-toggle" type="submit"  name="aceptar">Rechazar</button> 
     		 </label> 
	       	 </form>					       	
		</div>		
	</div>
	<!-- ENDS shadow -->
	 {% endfor %}   						       	 
</div>
<!-- ENDS post -->					
{% endblock %}
